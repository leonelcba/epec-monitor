name: Buscar Estacion Flores

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Buscar Estación Flores
        run: |
          npx --yes xactions search 'from:EpecOficial "Estación Flores"' --limit 20
