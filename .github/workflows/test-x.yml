name: Probar publicaciones EPEC

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Consultar últimas publicaciones de EPEC
        run: |
          npx --yes xactions tweets EpecOficial --limit 20
